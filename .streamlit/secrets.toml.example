# This app asks each visitor to enter their own OpenAI API key in the UI.
# You do not need to add OPENAI_API_KEY to Streamlit Cloud Secrets.
#
# Keep .streamlit/secrets.toml out of git if you create one for local experiments.
